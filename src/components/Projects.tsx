import React, { ReactElement } from 'react'

interface Props {
    
}

function Projects({}: Props): ReactElement {
    return (
        <div>
            Projects
        </div>
    )
}

export default Projects
